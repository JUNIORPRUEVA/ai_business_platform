# Nota: este .dockerignore se usa solo si el build-context es la raíz del repo.
# En EasyPanel, si el contexto es /backend, se usa backend/.dockerignore.

# Default: no filtrar nada desde raíz (para no romper plataformas que setean contexto distinto)
